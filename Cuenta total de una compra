#include "stdafx.h"
#include "conio.h"
#include <iostream>
using namespace std;

void main()
{int i,a,b,c,d;
 float x,y,z;
 do {do{cout<<"\nIngrese las clases de productos comprado:";
       cin>>a;}while(a>5);
	   z=0;
	   for(i=1;i<=a;i++)
	   { cout<<"\nIngrese cantidad del prudcto "<<i<<":";
	     cin>>b;
	     cout<<"\nIngrese precio del producto "<<i<<":";
	     cin>>x;
	     y=(b*x);
	     z=y+z;
	   }
	cout<<"\nLa cuenta total es:"<<z;
    cout<<"\nDesea realizar otra operacion?:\n 1.-si      2.-no\n Opcion:";
	cin>>d;system ("cls");
    }while(d==1);
	getch();
}
